<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/accentColor</item>
        <item name="colorPrimaryDark">@color/darkPrimaryColor</item>
        <item name="colorAccent">@color/darkPrimaryColor</item>
        <item name="backgroundColor">@color/lightPrimaryColor</item>
        <item name="cardBackgroundColor">@color/cardview_light_background</item>
        <item name="textColor">@color/nearlyBlack</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="switchText">"   Выключен"</item>
    </style>

    <style name="DarkAppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/black</item>
        <item name="backgroundColor">@color/gray</item>
        <item name="cardBackgroundColor">@color/nearlyBlack</item>
        <item name="textColor">@color/white</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="switchText">"   Включен"</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
    </style>

</resources>